cmake_minimum_required(VERSION 2.8)
project(myfilter)
set(SOURCES myfilter.cpp)
set(HEADERS myfilter.h)
add_library(myfilter STATIC ${SOURCES} ${HEADERS})
target_include_directories(myfilter PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})